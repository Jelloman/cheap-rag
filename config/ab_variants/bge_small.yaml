name: bge_small
description: "BGE Small (BAAI/bge-small-en-v1.5) — faster, lower quality, 384-dim"
embedding_model: BAAI/bge-small-en-v1.5
embedding_dimension: 384
top_k: 5
query_prefix: "Represent this sentence for searching relevant passages: "
# BGE document embeddings do not require a prefix
source_persist_directory: ./data/vector_db
source_collection_name: cheap_metadata_v1
metadata:
  description: BGE Small - faster, lower quality
